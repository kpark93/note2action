// App-wide constants shared across the web and ai apps.
// Path: web app / ai app → [this file].

/** The product's display name, so a rename only happens in one place. */
export const APP_NAME = "note2action";
