# Copy to `.env` (gitignored) and fill in real values. Vite only exposes
# variables prefixed VITE_ to the browser bundle — never put secrets here,
# the publishable key is public by design (the secret key stays in the API).
VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace-me
